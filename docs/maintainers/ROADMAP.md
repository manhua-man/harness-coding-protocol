# Roadmap

**Direction:** Git + IDE plugin. User path = `/harness-init` and C5 commands in **target repos**. No npm package, no terminal CLI, no in-repo smoke scripts.

## Open

- [ ] Optional: split lean plugin distribution vs full protocol repo (not started)

## Not planned

- npm registry / `package.json` user install
- Terminal CLI (`harness detect`, machine plans)
- In-repo `scripts/` smoke or GitHub Actions regression
- Per-IDE bundle doc trees (removed — skills + README are enough)
