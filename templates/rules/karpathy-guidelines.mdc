# Claude Code 编码指南

> 基于 Karpathy 的 LLM 编码观察

---

## 核心原则

| 原则 | 何时使用 |
|------|----------|
| **Think Before Coding** | 遇到模糊需求、有多种解释时 |
| **Simplicity First** | 设计新功能、创建抽象前 |
| **Surgical Changes** | 编辑现有代码时 |
| **Goal-Driven Execution** | 修复 bug、添加功能时 |
| **Explicit Dependencies** | 依赖关系不清晰、难以测试时 |

---

## Think Before Coding

- 显式声明假设
- 呈现多种解释
- 在必要时反驳
- 困惑时停止

## Simplicity First

- 不添加超出需求的功能
- 不为单次使用创建抽象
- 不添加未被要求的"灵活性"
- 如果 200 行能变成 50 行，重写它

## Surgical Changes

- 不要"改进"相邻代码
- 不要重构没坏的东西
- 匹配现有风格
- 只清理自己造成的孤儿

## Goal-Driven Execution

- 定义成功标准
- 先写测试/验证
- 循环直到验证通过

## Explicit Dependencies

- 依赖通过参数或 DI 传入，不用隐式全局状态
- 避免 `this` 上挂未声明的业务数据
- 环境变量不直接引用，通过配置对象传入
- 新逻辑默认可测

---

## 常见错误模式

| 原则 | 反模式 | 修复 |
|------|--------|------|
| **三思后行** | 默认可选文件格式、字段、范围 | 明确列出假设，要求澄清 |
| **简洁优先** | 单一折扣计算用策略模式 | 在真正需要复杂度之前，只用一个函数 |
| **手术改动** | 重排引号、添类型提示、同时修无关错误 | 只更换能解决报告问题的行 |
| **目标驱动** | "我会审查并改进代码" | "写测试漏洞X → 让它通过 → 验证没有回归" |
| **显式依赖** | `this` 挂隐式数据、环境变量直接引用 | 通过参数/DI 传入，类型约束 |

详细示例见 [steering/karpathy-examples.md](../steering/karpathy-examples.md)
