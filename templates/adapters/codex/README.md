# Codex Adapter

Codex reads the repository root truth directly, so this adapter is documentation-only.

## Recommended Inputs

- `AGENTS.md`
- `CLAUDE.md`
- `steering/`

## Why No Mirror

Codex does not need a dedicated compatibility layer in this starter kit. The repository root files are already the canonical inputs.
